#include <iostream>
#include <vector>

int main(void)
{
    int target; std::cin >> target;
    int n; std::cin >> n;
    std::vector<int> vec(n);

    for (auto element : vec)
    {
        std::cin >> element;
    }

    int ans = 
    

    return 0;
}
